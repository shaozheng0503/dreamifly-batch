# 把浏览器登录 dreamifly.com 后的整行 Cookie 粘贴到 config/cookie.txt（复制本文件为 cookie.txt 再编辑）。
# 获取方式：浏览器登录 dreamifly.com → 开发者工具(F12) → Network → 刷新后随便点一个请求
#           → Request Headers 里找到 Cookie，复制整行。
# 说明：
#   - 以 # 开头的行和空行会被忽略，脚本取第一条非注释行作为 Cookie。
#   - gpt-image-2 模型必须有有效登录态，否则会返回 401。
#   - cookie 是你的登录凭证，已被 .gitignore 排除，请勿提交到仓库。
#
# 示例（无效占位，请替换为你自己的）：
# session=xxxxxxxxxxxx; token=yyyyyyyyyyyy; other=zzzz
