# Gotchas

- Never run video, `nano-banana-2`, or any 5+ credit item without explicit approval.
- Never print, commit, or summarize `config/cookie.txt`.
- Never auto-retry video jobs.
- Never retry 401/402 blindly; ask for a fresh cookie or more credits first.
- `Qwen-Image-Edit` requires `img=`.
- `Wan2.2-I2V-Lightning` requires exactly one source image.
- Use `--summary` / `results.jsonl` for reporting, not memory.
